# DESKTOP-09：Windows 发布

## 目标

交付经过验证的 `win-x64` self-contained ZIP 发布流程。

## 范围与退出

在干净 Windows 10/11 环境解压并验证工作台、导入、分析、调整、生成、取消、历史和已通过 POC 的预览/回退。发布包不含 Blender、真实样本、凭据、作业数据或机器绝对路径。签名策略单独确认；不得实现自动更新、在线安装器、云同步或 IDTS 接入。记录 runtime、包大小、校验和脱敏手工验收证据即退出。
