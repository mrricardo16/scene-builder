# DESKTOP-03：新建转换向导

## 目标

提供未来 DXF-only 新建作业向导：输入、规则、作业目录和预检摘要四步。

## 验收

- DXF 可进入预检；DWG 和未知格式明确显示“暂不支持/无效”，不能创建或启动作业。
- 向导只创建受控请求，不拼接 CAD、SceneDraft 或 Blender 流水线。
- 作业根限制在未来本地存储策略；不得指向仓库根、`src/` 或 `tests/`。

## 前置与非目标

依赖 DESKTOP-02；真实启动转换依赖 SB-10.5 与 DESKTOP-04。本卡不声明 DWG 支持。
