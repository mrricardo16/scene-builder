# DESKTOP-03：参数调整工作台

## 目标

为 `ConversionPlanDraft` 提供受控参数编辑、校验、保存修订与冻结入口。

## 范围与退出

支持已有单位确认、原点、Z Offset、平面旋转、修复、规则、现有高度、显式资产绑定和输出参数。每项显示范围与验证错误；不支持字段列为 Planned。不得猜测单位、重写分类、匹配任意目录 GLB 或修改原始 CAD。修订不可覆盖且 Build 只能接收冻结计划即退出。
