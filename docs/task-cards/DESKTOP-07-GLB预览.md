# DESKTOP-07：GLB 预览

## 目标

在 DESKTOP-00 的结论基础上，预览当前作业中已经验证的 GLB，或提供确定的外部查看器回退。

## 验收

- 只允许加载作业根内、已验证且由相对产物标识定位的 GLB。
- 内嵌 Viewer 支持旋转、缩放、重置、加载错误和关闭释放；.NET/JS 通信受限。
- 若技术验证不通过，清晰调用外部 GLB 查看器或已配置 Blender，不伪装为内嵌预览成功。

## 非目标

不预览任意路径，不打包 Blender，不实现 Cesium 或 3D Tiles。
